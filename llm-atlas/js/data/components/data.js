/**
 * Data plane: corpora, governance, filtering, tokenizer training, packing and sharding.
 * Ids and order follow manifest.js. Reference ids follow references.js.
 */
export const DATA_COMPONENTS = [
  {
    id: 'raw-corpora',
    name: 'Raw corpora',
    subtitle: 'The unprocessed text (and code) sources a pre-training run starts from, before any filtering.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['source data', 'crawl', 'web dump', 'pretraining corpus'],
    keywords: ['Common Crawl', 'WARC', 'WET', 'trafilatura', 'text extraction', 'HTML boilerplate', 'code repositories', 'books', 'FineWeb', 'DCLM'],
    summary: "Raw corpora are the collected inputs of a pre-training pipeline: web crawls, code repositories, books, papers, forums, and licensed or synthetic text. They arrive in heterogeneous formats (HTML, WARC/WET, PDF, git trees) and are converted to plain UTF-8 documents with source metadata before anything else happens.",
    purpose: 'Provide a large, diverse token budget. Nearly every downstream quality property (duplication rate, contamination, language mix, toxicity) is inherited from what is collected here and how text is extracted.',
    mechanics: "Web sources are commonly ingested from archived crawls (for example Common Crawl WARC files) and passed through an HTML text extractor that strips navigation, ads and boilerplate while keeping main-content paragraphs; extractor choice measurably changes downstream model quality, as the FineWeb and DCLM reports note. Each document is normalized to UTF-8, language-identified (fastText-style classifiers are common), and stored with provenance fields such as URL, crawl date, source name, and license tag. Code corpora are extracted from repository snapshots, keeping file paths and language labels. Documents are written to columnar or line-delimited shards (Parquet, JSONL, or similar) with a stable document id that later stages use for dedup and traceability. Volume at this stage is typically far larger than what is trained on; filtering and dedup downstream commonly discard the majority of raw tokens.",
    input: 'Heterogeneous source files (HTML/WARC, PDF, source trees, licensed dumps).',
    output: 'Plain-text documents with provenance metadata, written to shards.',
    memory: 'Petabyte-scale on object storage for large web crawls; nothing lives on accelerators at this stage.',
    compute: 'CPU-bound extraction and language identification over billions of documents; commonly a distributed batch job (Spark, Ray, Dask or custom).',
    communication: 'Object-storage I/O dominates; shard files are read once and rewritten.',
    knobs: ['Source selection and source weights', 'Text extractor and boilerplate rules', 'Language-ID threshold', 'Crawl date range', 'Minimum document length'],
    metrics: ['Raw token count per source', 'Language distribution', 'Extractor yield (chars kept / chars fetched)', 'Documents per shard'],
    failureModes: ['Boilerplate leaking into main text', 'Encoding corruption (mojibake) from mis-detected charsets', 'Silent inclusion of non-licensed or opted-out content', 'Source over-representation (one domain dominating a crawl)'],
    trainingVsInference: 'Only relevant to training. Inference never touches raw corpora.',
    related: ['data-governance', 'quality-filtering', 'deduplication', 'dataset-mixture', 'tokenizer-training'],
    references: ['fineweb', 'dclm'],
  },
  {
    id: 'data-governance',
    name: 'Data governance',
    subtitle: 'Licensing, opt-out honoring, provenance tracking and audit trail for every document that reaches training.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['data provenance', 'licensing', 'opt-out', 'data lineage'],
    keywords: ['robots.txt', 'license classification', 'consent', 'data lineage', 'audit', 'takedown', 'copyright', 'datasheets'],
    summary: 'Data governance is the set of policies and records that decide which sources may be used, under what terms, and how each document can be traced back to its origin. It runs as both a gate (exclude what cannot be used) and a ledger (record what was used).',
    purpose: 'Make training data legally and ethically defensible, and make it possible to answer "was document X in the training set" and to remove content after the fact.',
    mechanics: "Sources are classified by license or terms (public domain, permissive, share-alike, proprietary, unknown) and by consent signals such as robots.txt directives, AI-specific opt-out headers or lists, and publisher agreements; documents failing policy are dropped before filtering. Provenance fields (source, URL, crawl timestamp, license tag, extractor version) are attached to each document id and carried through every later stage, so a final training shard can be joined back to its origins. Takedown or opt-out requests are applied as exclusion lists keyed on URL, domain or content hash, and are re-run against derived artifacts. The mixture and shard manifests are versioned so that a given model checkpoint maps to an exact, reproducible data snapshot. Many teams also publish a datasheet or data card describing composition and known limitations.",
    input: 'Raw documents with source metadata; policy rules; opt-out and takedown lists.',
    output: 'Policy-compliant documents with provenance; an auditable manifest of what was included and excluded.',
    memory: 'Metadata tables are small relative to text but must be retained for the life of the model.',
    compute: 'Cheap per document; cost is dominated by maintaining and re-applying exclusion lists.',
    knobs: ['Allowed license classes', 'Opt-out signal sources honored', 'Exclusion-list refresh cadence', 'Retention period for lineage records'],
    metrics: ['Fraction of tokens by license class', 'Opt-out hits per snapshot', 'Time to apply a takedown to derived artifacts'],
    failureModes: ['Provenance lost after a transform (cannot trace a shard back)', 'Opt-outs applied to raw data but not to already-derived shards', 'License inferred from domain rather than from the actual document', 'Unversioned mixture, so a checkpoint cannot be reproduced'],
    trainingVsInference: 'A training-time concern; at inference the same lineage records are what make attribution or takedown questions answerable.',
    related: ['raw-corpora', 'quality-filtering', 'pii-secret-filtering', 'dataset-mixture', 'sharding'],
    references: ['fineweb', 'dclm'],
  },
  {
    id: 'quality-filtering',
    name: 'Quality filtering',
    subtitle: 'Heuristic rules and learned classifiers that drop low-value documents before training.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['quality filter', 'document filtering', 'classifier filtering', 'heuristic filtering'],
    keywords: ['Gopher rules', 'C4 filters', 'fastText classifier', 'perplexity filtering', 'educational quality', 'FineWeb-Edu', 'DCLM', 'toxicity filter'],
    summary: 'Quality filtering removes documents that are unlikely to help a language model: boilerplate, machine-generated spam, truncated pages, lists of symbols, or off-target content. It usually combines cheap heuristic rules with a learned classifier scored on each document.',
    purpose: 'Raise tokens-per-quality so a fixed compute budget is spent on informative text. Filtering choices are among the largest levers on downstream benchmark results for a fixed model size.',
    mechanics: "Heuristic stage: per-document statistics such as mean word length, fraction of alphabetic characters, symbol-to-word ratio, ratio of lines ending in punctuation, duplicated-line fraction, presence of stop words and document length are compared to thresholds (the Gopher and C4 rule sets are common starting points), and failing documents are dropped. Classifier stage: a small model (a fastText linear classifier or a fine-tuned small Transformer) is trained to separate a positive set (for example curated reference text or LLM-labelled 'educational' pages) from random crawl, and each document receives a score; documents below a threshold are removed or down-weighted. Perplexity filtering against a small n-gram or neural LM is another signal, with both very low and very high perplexity treated as suspicious. Filters are commonly applied per language with language-specific thresholds. The DCLM and FineWeb work shows that a model-based filter on top of heuristics is a stronger lever than heuristics alone, and that thresholds must be tuned against downstream evals rather than fixed a priori.",
    input: 'Deduplicated or raw documents with language labels.',
    output: 'A filtered document set plus per-document quality scores retained for mixture weighting.',
    compute: 'Heuristics are trivially cheap; a neural classifier over billions of documents is a substantial CPU or GPU batch job, but still small compared with the training run it feeds.',
    knobs: ['Heuristic thresholds (per language)', 'Classifier positive-set definition', 'Score cut-off or keep-fraction', 'Whether scores also feed mixture weights', 'Toxicity filter threshold'],
    metrics: ['Keep rate per source', 'Downstream eval delta on ablation runs at small scale', 'Classifier precision on a held-out labelled set', 'Domain diversity after filtering'],
    failureModes: ['Over-filtering removes code, math or non-English text that scores poorly on English-centric heuristics', 'Classifier positive set biases the corpus toward one style (e.g. encyclopedic prose)', 'Threshold chosen on raw score distribution rather than downstream evals', 'Filter drift when the classifier is retrained without re-validating thresholds'],
    trainingVsInference: 'Training-time only; the same classifier ideas reappear in post-training data curation.',
    related: ['raw-corpora', 'deduplication', 'pii-secret-filtering', 'contamination-detection', 'dataset-mixture', 'instruction-dataset'],
    references: ['fineweb', 'dclm'],
    major: true,
  },
  {
    id: 'deduplication',
    name: 'Deduplication',
    subtitle: 'Exact and near-duplicate removal at document and substring level, commonly via hashing and MinHash/LSH.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['dedup', 'near-dedup', 'fuzzy dedup', 'MinHash', 'LSH'],
    keywords: ['MinHash', 'locality-sensitive hashing', 'Jaccard similarity', 'shingles', 'suffix array', 'exact hash', 'URL dedup', 'Bloom filter'],
    summary: 'Deduplication removes repeated content so the model does not see the same text many times. Exact dedup catches identical documents; fuzzy dedup catches near-copies (mirrors, templated pages, minor edits) using set-similarity over n-gram shingles.',
    purpose: 'Reduce memorization and wasted compute, and prevent a small set of heavily mirrored documents from dominating the token budget. Repeated data is commonly found to hurt more than an equal amount of fresh data helps.',
    mechanics: "Exact stage: each document (after normalization such as lower-casing and whitespace collapse) is hashed (for example with a 64-bit or 128-bit non-cryptographic hash); duplicates of a hash are dropped, keeping one representative, and URL-level dedup is applied where available. Fuzzy stage: a document is shingled into overlapping n-grams (5-grams of words are common), and MinHash computes `k` hash functions over the shingle set, giving a signature whose per-position equality probability approximates Jaccard similarity. Signatures are split into `b` bands of `r` rows (`k = b × r`); documents sharing any band are candidate pairs, so the effective similarity threshold is about `(1/b)^(1/r)`, with thresholds near 0.7 to 0.8 common. Candidate pairs are grouped with a union-find into clusters and one document per cluster is kept. Substring-level dedup (suffix-array search for long repeated spans, for example 50+ tokens) removes repeated boilerplate embedded inside otherwise distinct documents. Dedup scope matters: within-source, cross-source, and cross-snapshot dedup give different results, and FineWeb reports that aggressive global dedup across crawl snapshots can remove useful data, so per-snapshot dedup is one practical choice.",
    input: 'Filtered or raw documents with stable ids.',
    output: 'A document set with duplicates removed, plus cluster membership for auditing.',
    memory: 'MinHash signatures are `k × 4 or 8 bytes` per document; for billions of documents the signature table and union-find state are the dominant memory cost of the whole data pipeline.',
    compute: 'Hashing is linear in total tokens; LSH avoids the quadratic pairwise comparison. Suffix-array construction is heavier and is often run only on subsets.',
    communication: 'Banding requires a shuffle (group by band hash) across the cluster; this is the communication-heavy step.',
    knobs: ['Shingle size n', 'Number of hashes k and band/row split (b, r)', 'Similarity threshold', 'Dedup scope (per source, global, per snapshot)', 'Substring dedup minimum span length'],
    metrics: ['Fraction of documents/tokens removed', 'Duplicate cluster size distribution', 'Memorization rate on canary strings after training', 'Estimated Jaccard of kept near-neighbours'],
    failureModes: ['Threshold too low removes legitimate templated content (licenses, code headers) wholesale', 'Keeping a random cluster representative drops the highest-quality version', 'Dedup run before quality filtering keeps a low-quality copy over a good one', 'Signature memory exceeding the driver node on large corpora'],
    trainingVsInference: 'Training-time only. Note that vLLM-style prefix caching at inference is a different mechanism (reuse of computed KV blocks across requests), not text deduplication.',
    related: ['raw-corpora', 'quality-filtering', 'contamination-detection', 'dataset-mixture', 'sharding'],
    references: ['fineweb', 'dclm'],
    major: true,
  },
  {
    id: 'pii-secret-filtering',
    name: 'PII / secret filtering',
    subtitle: 'Detection and removal or redaction of personal data and credentials before text reaches the model.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['PII scrubbing', 'secret scanning', 'redaction', 'credential filtering'],
    keywords: ['regex', 'NER', 'email', 'phone number', 'IP address', 'API keys', 'private keys', 'entropy', 'detect-secrets', 'presidio'],
    summary: 'PII and secret filtering finds personal identifiers (emails, phone numbers, addresses, IDs) and credentials (API keys, tokens, private keys) in documents and either drops the document, masks the span, or replaces it with a placeholder token.',
    purpose: 'Reduce the chance the model memorizes and later reproduces private data or working secrets, and satisfy governance policy.',
    mechanics: "A first pass uses pattern matchers: regular expressions for emails, phone formats, IPv4/IPv6, credit-card-shaped digit runs with checksum validation, and vendor-specific key prefixes (for example the shape of cloud provider or payment API keys). A second pass uses entropy tests on high-entropy strings inside code (base64 blobs, hex strings above a length threshold) and named-entity recognition models for person names and addresses in prose. Matches are handled by policy: replace with a fixed placeholder (e.g. `<EMAIL>`), drop the line, or drop the whole document when the density of matches is high. Code corpora commonly get the strictest secret rules; scrubbing is applied before tokenizer training so placeholder tokens can be learned as single units if desired. Every rule is versioned and its hit counts are logged so precision can be audited on samples.",
    input: 'Documents (prose and code).',
    output: 'Documents with PII/secret spans removed or replaced, plus per-rule hit statistics.',
    compute: 'Regex passes are cheap; NER models over a full corpus are a meaningful CPU/GPU batch cost and are sometimes applied only to selected sources.',
    knobs: ['Rule set and regex list', 'Entropy threshold and minimum length for secret candidates', 'Redact vs drop policy', 'Placeholder vocabulary', 'Per-source strictness'],
    metrics: ['Hits per million tokens per rule', 'Precision on an audited sample', 'Canary-secret extraction rate after training', 'Documents dropped by density rule'],
    failureModes: ['Regex false positives destroying code (hex colours, hashes in tests treated as secrets)', 'Placeholder tokens over-represented, so the model emits them in generation', 'Names removed from public figures or citations, harming factual recall', 'Secrets that only appear across line breaks slipping through line-based scanning'],
    trainingVsInference: 'Training-time on the corpus; separate runtime-side output filters are a different control and do not undo memorization.',
    related: ['data-governance', 'quality-filtering', 'deduplication', 'tokenizer-training', 'safety-tuning'],
    references: ['fineweb'],
  },
  {
    id: 'contamination-detection',
    name: 'Contamination detection',
    subtitle: 'Finding evaluation-benchmark text inside the training corpus, commonly by n-gram overlap, and removing it.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['decontamination', 'benchmark leakage', 'test-set contamination', 'overlap detection'],
    keywords: ['n-gram overlap', '13-gram', 'benchmark', 'held-out', 'leakage', 'GSM8K', 'MMLU', 'HumanEval', 'canary strings'],
    summary: 'Contamination detection checks whether benchmark questions, answers or test inputs appear verbatim or near-verbatim in training data. Contaminated documents are removed (decontamination) or flagged so affected benchmark scores can be reported separately.',
    purpose: 'Keep evaluation results meaningful. A model that has seen a test set scores well on it without the underlying capability, which corrupts every decision made from that score.',
    mechanics: "The benchmark corpus (questions, answers, and sometimes prompts and few-shot exemplars) is normalized (lower-cased, punctuation and whitespace collapsed) and turned into a set of n-grams; sizes around 8 to 13 tokens or words are commonly used, with the choice trading recall against false matches on common phrases. Each training document is scanned for these n-grams; a document is flagged if any n-gram matches (strict) or if the fraction of its n-grams that match exceeds a threshold (lenient). Flagged documents are removed from training, or the affected benchmark examples are marked 'contaminated' and scores are reported on the clean subset. Paraphrase-level contamination (translated or reworded test items) is not caught by n-grams; embedding-similarity checks and canary-string insertion in benchmarks are complementary tools. Decontamination should be re-run whenever a new benchmark is added to the eval suite, which is why the pipeline keeps document ids stable.",
    input: 'Training documents; the text of every benchmark in the evaluation suite.',
    output: 'Decontaminated training set; a per-benchmark list of contaminated example ids.',
    compute: 'Linear scan over the corpus with a hash set of benchmark n-grams; cheap relative to filtering and dedup.',
    knobs: ['n-gram size', 'Match threshold (any vs fraction)', 'Normalization rules', 'Which benchmark fields are included (questions only vs questions + answers)', 'Remove document vs flag benchmark item'],
    metrics: ['Contaminated documents per benchmark', 'Score delta between full and clean benchmark subsets', 'False-positive rate on generic phrases'],
    failureModes: ['n too small, so common phrases flag huge numbers of documents', 'Benchmarks added after decontamination are silently contaminated', 'Paraphrased or translated test items pass through', 'Removing documents that merely cite a benchmark (papers) without containing its items, if policy is too strict'],
    trainingVsInference: 'Training-time only; it exists to protect the evaluation stage.',
    related: ['quality-filtering', 'deduplication', 'dataset-mixture', 'evaluation', 'instruction-dataset'],
    references: ['fineweb', 'dclm', 'llama3'],
  },
  {
    id: 'dataset-mixture',
    name: 'Dataset mixture',
    subtitle: 'Per-source sampling weights (and upsampling) that define what fraction of training tokens comes from where.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['data mix', 'mixture weights', 'sampling proportions', 'curriculum'],
    keywords: ['upsampling', 'epochs per source', 'domain weights', 'DoReMi', 'annealing', 'curriculum', 'token budget', 'Chinchilla'],
    summary: 'The dataset mixture assigns each source (web, code, math, books, multilingual, synthetic) a sampling weight so that the training stream has the intended composition. Sources smaller than their target share are upsampled (seen more than once); large sources are subsampled.',
    purpose: 'Control what the model spends its token budget on. Mixture is a first-order determinant of capability profile (code vs prose vs math vs languages) and of how many total tokens are available.',
    mechanics: "Given target proportions `p_s` per source and a total token budget `N`, each source contributes about `p_s × N` tokens; if that exceeds the source's size, the source is repeated (`epochs_s = p_s × N / size_s`), and repetition beyond a few epochs is commonly reported to yield diminishing returns. Weights are set by small-scale ablations, by heuristics (down-weight web, up-weight code and math), or by learned methods that adjust proportions from proxy-model losses. Many recent recipes use multiple phases: a broad mixture for most of training and an 'annealing' phase near the end that up-weights high-quality and domain-specific data while the learning rate decays. Quality scores from filtering can feed into finer-grained weights (e.g. up-weight top-scoring buckets). On total budget, the Chinchilla analysis suggests compute-optimal training around 20 tokens per parameter for its setting, but production models are commonly trained well beyond that to reduce inference cost; treat any tokens-per-parameter figure as setting dependent. The mixture is materialized either by pre-mixing shards or by a sampler that draws from per-source streams at run time with a fixed seed.",
    input: 'Filtered, deduplicated, decontaminated per-source document sets with sizes.',
    output: 'A weighted, shuffled document stream (or pre-mixed shards) with recorded weights and epoch counts.',
    memory: 'Negligible on accelerators; the mixture is a sampling policy over shards.',
    compute: 'Choosing weights by ablation costs many small training runs; applying them is free.',
    knobs: ['Per-source proportions', 'Maximum epochs per source', 'Phase schedule (broad vs annealing)', 'Quality-bucket weights', 'Total token budget'],
    metrics: ['Realized token share per source', 'Epochs per source', 'Per-domain validation loss during training', 'Downstream evals at proxy scale'],
    failureModes: ['Small high-quality source upsampled so far it is memorized', 'Web share so high that code/math capability stalls', 'Annealing data overlapping with evaluation sets', 'Mixture changed mid-run without versioning, breaking reproducibility'],
    trainingVsInference: 'Training-time only.',
    related: ['quality-filtering', 'deduplication', 'contamination-detection', 'sequence-packing', 'sharding', 'lr-scheduler'],
    references: ['chinchilla', 'llama3', 'dclm'],
  },
  {
    id: 'tokenizer-training',
    name: 'Tokenizer training',
    subtitle: 'Learning the subword vocabulary (BPE or unigram) and merge rules that map text to integer ids.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['BPE training', 'vocabulary learning', 'subword model', 'SentencePiece training'],
    keywords: ['BPE', 'byte-level BPE', 'unigram LM', 'SentencePiece', 'tiktoken', 'Hugging Face tokenizers', 'vocabulary size', 'byte fallback', 'pre-tokenization regex', 'special tokens'],
    summary: 'Tokenizer training builds the fixed vocabulary the model will use for its whole life. Byte-pair encoding iteratively merges the most frequent adjacent symbol pairs into new tokens; the unigram method starts from a large candidate set and prunes it to maximize corpus likelihood. The result is a lookup of tokens plus rules for splitting arbitrary text.',
    purpose: 'Compress text into a sequence of ids that is short (fewer tokens per character means cheaper training and inference) while keeping every possible input representable and the vocabulary small enough for the embedding and LM-head matrices.',
    mechanics: "Text is first split by a pre-tokenization regex into chunks (for example separating words, digits in groups, punctuation and leading spaces) so that merges never cross those boundaries; the regex is part of the tokenizer definition and changes how numbers and code tokenize. BPE: start from bytes (byte-level BPE, as in GPT-2 style tokenizers) or characters, count adjacent pairs across the training sample, merge the most frequent pair into a new symbol, record the merge, and repeat until the target vocabulary size is reached; the ordered merge list is the model. Unigram (SentencePiece default): start from a large seed vocabulary, compute the likelihood of the corpus under a unigram model where each piece has a probability, and iteratively remove pieces whose removal costs least, until the target size is reached; encoding then picks the Viterbi-best segmentation. Byte fallback guarantees coverage: characters not in the vocabulary are represented by their UTF-8 bytes (byte-level BPE has this by construction; SentencePiece adds 256 byte tokens when byte_fallback is enabled). Special tokens (BOS, EOS, padding, chat-role markers) are reserved ids outside the learned merges. Vocabulary size is a trade-off: larger vocabularies (128K-class tokenizers, for example Llama 3's 128K) improve compression, especially for multilingual and code text, at the cost of a bigger `[vocab, d_model]` embedding and `[d_model, vocab]` LM head. The training sample should match the intended mixture; a tokenizer trained on English prose fragments code and other scripts into many bytes.",
    input: 'A representative text sample (commonly a few gigabytes to tens of gigabytes) drawn from the final mixture; target vocabulary size; pre-tokenization rules; special-token list.',
    output: 'A tokenizer artifact: vocabulary (token → id), merges or piece scores, pre-tokenization regex, normalization rules, special tokens.',
    memory: 'The artifact is a few megabytes. Its size drives the embedding and LM-head parameter counts: `vocab × d_model` each (shared if tied).',
    compute: 'BPE training is a CPU job over the sample; efficient implementations use priority queues over pair counts. It is negligible relative to model training but is run once and then frozen.',
    knobs: ['Vocabulary size', 'BPE vs unigram', 'Pre-tokenization regex (digit grouping, whitespace handling)', 'Byte fallback on/off', 'Normalization (NFKC, lower-casing) rules', 'Training-sample mixture', 'Reserved special tokens'],
    metrics: ['Compression: bytes per token or tokens per word, per language and for code', 'Fertility (tokens per word) on held-out text', 'Fraction of byte-fallback tokens in target languages', 'Unused-token count after training'],
    failureModes: ['Vocabulary trained on a different mixture than the model, producing poor compression for code or non-Latin scripts', 'Number tokenization inconsistent (digits merged unpredictably), hurting arithmetic', 'Changing the tokenizer after pre-training invalidates the embedding matrix', 'Whitespace handling that makes leading-space and no-space variants of a word unrelated tokens', 'Special tokens that can be produced from user text (injection)'],
    trainingVsInference: 'Trained once before pre-training and frozen. Inference uses the same artifact for encode (text → ids) and decode (ids → text); it is never retrained per request.',
    related: ['tokenization', 'token-embeddings', 'lm-head', 'vocabulary-logits', 'tokenizer-encode', 'tokenizer-decode', 'chat-template'],
    references: ['bpe', 'sentencepiece', 'llama3'],
    major: true,
  },
  {
    id: 'tokenization',
    name: 'Tokenization',
    subtitle: 'Applying the frozen tokenizer to turn every training document from a string into a sequence of integer ids.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['encode', 'text to ids', 'subword segmentation'],
    keywords: ['tokenizer encode', 'BPE merges', 'tiktoken', 'Hugging Face tokenizers', 'Rust tokenizers', 'special tokens', 'BOS', 'EOS', 'uint16', 'uint32'],
    summary: 'Tokenization is the bulk pass that converts the final text corpus to token ids using the trained tokenizer. Each document becomes an integer array, usually with an end-of-document token appended, stored in a compact binary format ready for packing.',
    purpose: 'Move the expensive string processing off the training critical path. Training loops read integer arrays and never touch text.',
    mechanics: "For each document: apply normalization, split with the pre-tokenization regex, then encode each chunk (BPE applies merges in rank order; unigram runs the Viterbi segmentation), and concatenate the ids. Special tokens such as BOS at the start and EOS at the end mark document boundaries for the packing stage. Ids are stored as `uint16` when the vocabulary is below 65,536 and `uint32` otherwise (a 128K vocabulary needs 32-bit or a packed 17-bit format), together with an index of document offsets so any document can be located without scanning. The pass is parallel over shards and its output is written in the same shard layout. Tokenizer version is recorded in the shard metadata; a mismatch between the tokenizer used here and the one used at inference produces garbage. This is tokenizer encoding (string → ids); it is distinct from tokenizer decoding (ids → string), and both are distinct from generation decoding (choosing the next token from logits).",
    input: 'Mixed, filtered documents as UTF-8 strings; the frozen tokenizer.',
    output: 'Per-shard integer arrays with a document-offset index and a total token count.',
    tensorShape: 'A ragged collection of 1-D int arrays, one per document; the total count is the run’s token budget.',
    memory: 'At 2 or 4 bytes per token, a multi-trillion-token corpus is several terabytes on disk; it is streamed, not loaded.',
    compute: 'CPU-bound; a fast Rust or C++ tokenizer processes on the order of megabytes per second per core, so the pass is run across many workers.',
    knobs: ['Whether to add BOS/EOS', 'Integer width', 'Shard size', 'Parallelism per shard'],
    metrics: ['Total tokens produced', 'Tokens per byte per source', 'Throughput (tokens per second per core)', 'Tokenizer version hash recorded'],
    failureModes: ['Tokenizer version drift between corpus and model', 'Missing EOS so packing cannot find document boundaries', 'Integer overflow when vocabulary exceeds the chosen width', 'Special-token strings in user data encoded as the special id'],
    trainingVsInference: 'Training: an offline batch pass over the whole corpus. Inference: the same encode step runs per request on the prompt (see tokenizer-encode in the runtime).',
    related: ['tokenizer-training', 'sequence-packing', 'sharding', 'token-embeddings', 'tokenizer-encode', 'tokenizer-decode'],
    references: ['bpe', 'sentencepiece'],
  },
  {
    id: 'sequence-packing',
    name: 'Sequence packing',
    subtitle: 'Concatenating variable-length documents into fixed-length training windows, with separators or document masks.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['packing', 'concatenation', 'document masking', 'sample packing'],
    keywords: ['EOS separator', 'block-diagonal mask', 'cross-document attention', 'position ids reset', 'varlen attention', 'cu_seqlens', 'FlashAttention varlen', 'padding waste'],
    summary: 'Sequence packing fills each fixed-length training window (for example 4K or 8K tokens) with several documents back to back, instead of padding each document to the window length. Boundaries are marked with a separator token and optionally enforced by an attention mask so tokens cannot attend across documents.',
    purpose: 'Keep accelerator utilization high. Padding wastes FLOPs and memory; packing makes every position in every batch a real training token.',
    mechanics: "The tokenized stream is read in order and cut into windows of length `T` (the training sequence length); a document that straddles a window boundary is either split (its tail continues in the next window) or the window is filled with a shorter document (bin-packing variants). An EOS or document-separator token sits between documents, so the model learns that context resets there. Two boundary policies exist: (1) plain concatenation, where causal attention lets tokens of document B see document A, which is cheap and was standard for a long time; (2) document masking, where the attention mask is block-diagonal within the window so each token attends only to its own document, and position ids restart at 0 per document (RoPE positions follow the reset). Policy (2) is implemented efficiently with variable-length attention kernels that take cumulative sequence lengths (`cu_seqlens`) rather than a dense mask, and is reported in the Llama 3 paper to matter for long-context training. The loss is computed at every position either way; some recipes exclude the separator token from the loss. Packing is deterministic given the stream order so that a run can be resumed at the same sample.",
    input: 'Tokenized document stream.',
    output: 'Fixed-length id arrays (plus, with document masking, per-window segment boundaries and position ids).',
    tensorShape: '`[batch, T]` int ids, with optional `cu_seqlens` for masked attention.',
    memory: 'No padding, so activation memory per batch is fully used. Document masking with varlen kernels adds no dense `T × T` mask.',
    compute: 'Plain concatenation keeps standard attention cost `O(T²)` per window. Document masking with a varlen kernel reduces attention FLOPs to the sum over documents of `len²`.',
    knobs: ['Sequence length T', 'Split-across-window vs bin-pack', 'Separator token choice', 'Cross-document masking on/off', 'Position-id reset per document', 'Loss on separator tokens'],
    metrics: ['Padding fraction (should be ~0)', 'Mean documents per window', 'Fraction of documents split across windows', 'Attention FLOPs per window under masking'],
    failureModes: ['No separator token, so the model never learns context boundaries', 'Cross-document attention letting unrelated text pollute long-context behaviour', 'Position ids not reset while masking is on, wasting the position range', 'Non-deterministic packing that breaks checkpoint-resume reproducibility'],
    trainingVsInference: 'Training-only. At inference each request is its own sequence; the analogous efficiency mechanism is continuous batching of independent requests, not packing.',
    related: ['tokenization', 'sharding', 'causal-attention', 'rope', 'flash-attention', 'forward-pass', 'continuous-batching'],
    references: ['llama3', 'flashattention'],
  },
  {
    id: 'sharding',
    name: 'Sharding',
    subtitle: 'Splitting the packed token stream into files that parallel loaders read deterministically and can resume from.',
    subsystem: 'data',
    modes: ['system', 'pretraining'],
    aliases: ['data shards', 'data loader', 'dataset indexing', 'shuffle buffer'],
    keywords: ['Megatron indexed dataset', 'WebDataset', 'Parquet', 'memory-mapped', 'seeded shuffle', 'consumed samples', 'data-parallel rank', 'resume', 'prefetch'],
    summary: 'Sharding lays out the packed dataset as many files with an index, and defines the deterministic order in which each data-parallel rank reads them. It is what allows thousands of workers to stream different data at full bandwidth and to resume exactly where a checkpoint stopped.',
    purpose: 'Feed accelerators without stalls, guarantee every rank sees disjoint data, and make the training data order a pure function of (seed, step) so runs are reproducible and restartable.',
    mechanics: "The packed token array is stored in shards of a few hundred megabytes to a few gigabytes with an index file mapping sample id → (shard, offset); memory-mapped formats let a worker read sample `i` without parsing the file. A global sample order is generated from a seed (per-epoch permutation, often blockwise to preserve locality), and rank `r` of `D` data-parallel ranks takes samples `r, r + D, r + 2D, ...` (or contiguous slices) of that order, so shards are never read twice within an epoch. Resumption stores the number of consumed samples (or the step) in the checkpoint; on restart the loader skips to that sample index in the same permutation, which is why the order must be a function of seed and step, not of wall-clock or worker count. Loaders prefetch and pin host memory so the H2D copy of the next batch overlaps compute; with mixture sampling, per-source streams are advanced with their own deterministic cursors. Changing data-parallel size mid-run requires remapping consumed samples to the new rank layout.",
    input: 'Packed fixed-length sequences; mixture weights; seed; data-parallel world size.',
    output: 'Per-rank batches `[micro_batch, T]` delivered in a reproducible order.',
    tensorShape: '`[micro_batch, T]` int ids per rank per step.',
    memory: 'Host-side prefetch buffers of a few batches per rank; the dataset itself is memory-mapped or streamed from object storage.',
    compute: 'Negligible; the risk is I/O stalls, not FLOPs.',
    communication: 'Storage → host bandwidth per node must exceed `tokens_per_step × bytes_per_token / step_time`; no inter-rank communication is needed if the order is computed locally from the seed.',
    knobs: ['Shard size', 'Shuffle granularity (sample vs block)', 'Seed', 'Prefetch depth and workers per rank', 'Storage backend (local NVMe vs object store)'],
    metrics: ['Data-loader wait time per step', 'Read bandwidth per node', 'Duplicate-sample check across ranks', 'Resume correctness (same batch reproduced after restart)'],
    failureModes: ['Two ranks reading the same samples (world-size bug), silently halving effective data', 'Resume replaying or skipping data after a checkpoint', 'Loader stalls that show up as low accelerator utilization', 'Shards unevenly sized, creating stragglers'],
    trainingVsInference: 'Training-only; inference has no dataset, only per-request prompts.',
    related: ['sequence-packing', 'dataset-mixture', 'data-parallel', 'forward-pass', 'checkpoint-saving', 'gradient-accumulation'],
    references: ['megatron-lm', 'llama3'],
  },
];
